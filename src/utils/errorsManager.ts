interface errorsManager {
    error: any
}

export const errorsManager = (error) => {
    // switch (error) {
    //     case value:

    //         break;

    //     default:
    //         break;
    // }
}